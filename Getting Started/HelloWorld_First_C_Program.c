#include <stdio.h>  //header file used for using various libraries such as input/output functions

int main(){     //the main function is the head of every program, each and every program starts executing from the main() function
    printf("Hello, World!");    //this is the function in which --> printf(argument); here argument is that value what we want to print out.
    return 0;   //since the main function is of type 'int', therefore it must return a value,but return 0; here '0' determines that the program ran successfully.
}
